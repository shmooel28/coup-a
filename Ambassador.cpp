#include "Ambassador.hpp"
using namespace std;


namespace coup
{
    Ambassador::Ambassador(Game &x,string name) : Player(x,name)
    {
    }
    void Ambassador::transfer(Player a, Player b){cout<<"coup"<<endl;}
}